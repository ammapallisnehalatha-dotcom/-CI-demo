name: Deploy

on:
  push:
    branches:
      - agile

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Test
        run: echo "Deployment successful"